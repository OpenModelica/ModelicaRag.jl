# Julia
Manifest.toml
*.jl.cov
*.jl.*.cov
*.jl.mem

# Dotfiles and dot-directories (IDE settings, tool caches, local configs)
.*
# Keep tracked example/template dotfiles
!.gitignore
!.github
!.mcp.json.example
!.devcontainer

# Local config (keep the example)
config.toml

# Generated data / index database
/data/

# Compiled app bundle produced by `make build`
/build/

# Cloned dependency repos (legacy, no longer used)
/deps/repos/

# Editor
*.swp
*~
